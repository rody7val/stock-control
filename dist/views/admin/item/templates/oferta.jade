.tab-pane(
	class='{{currentPage == 2 ? "active" : ""}}'
)
	br
	.card
		h3.card-header Oferta #[span.text-muted 30%]
		.card-block
			h4.card-title #{item.name}
			p.card-text #{item.desc}
			a.btn.btn-primary(href='#') Ver más