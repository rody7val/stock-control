if session.user
	li.nav-item.dropdown
		a#user.nav-link.dropdown-toggle(
			href='/#'
			data-toggle="dropdown"
			aria-haspopup="true"
			aria-expanded="true"
		) !{session.user.username}
		.dropdown-menu(aria-labelledby='user')
			a.dropdown-item(href='/users/#{session.user._id}') Mi perfil
			a.dropdown-item(href='/admin') Administración
	li.nav-item
		a.nav-link(href='/session/logout') Salir
else
	li.nav-item
		a.nav-link(href='/session/login') Entrar
	li.nav-item
		a.nav-link(href='/users/new') Registrarse